#!/usr/bin/env sh

echo "🔍 Botbyte CLI checks for tests before pushing"
npm run test

if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Push aborted."
  exit 1
fi

echo "✅ All tests passed. Pushing to Botbyte CLI🎉..."
